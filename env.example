# Секреты приложения
JWT_SECRET=измените-эту-секретную-строку
ADMIN_CODE=admin2026

# --- Подключение к базе данных MySQL ---
# ЛОКАЛЬНО (своя MySQL на компьютере):
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sosed

# НА RAILWAY ничего сюда писать не нужно — добавьте в переменные веб-сервиса:
# DATABASE_URL = ${{ MySQL.MYSQL_URL }}
# (подробности в README)
